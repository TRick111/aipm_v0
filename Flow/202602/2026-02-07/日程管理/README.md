# 2026-02-07 日程管理（Flow）

このフォルダは `Stock/定型作業/日程管理/` の作業用（WIP）です。

## 今日やること（最短でBUSYブロック投入まで）
- Google Calendar API（OAuth）で `work` カレンダーに `BUSY` 予定を作成する最小スクリプトを用意
- 入力フォーマットを固定（日時/タイムゾーンのみでOK）

## 成果物
- `gcal_busy_blocker/`: GoogleカレンダーにBUSYブロックを入れるための最小Nodeスクリプト
- `request_format.md`: 依頼テンプレ（この形式で送ればブロックできる）

