# BL-0060 Claude Code アップデート: ユーザーへの確認事項

- 作成日: 2026-04-22
- 関連: BL-0060 / Cockpitタスク `206a5614`
- **回答方法**: 各質問の「✍️ 回答記入欄」に直接書き込んでください

## 背景

Mac 側はすでに最新版（v2.1.117）に到達済み。「アップデート」自体は不要で、課題は **配布形態の切替判断** のみ。

現状:
- インストール経路: `npm install -g`（Homebrew node 経由）
- バージョン: 2.1.117（=最新）
- 設定ファイル（`~/.claude/settings.json` など）は切替しても保持される

---

## Q1. Mac側の配布形態を切り替えるか？ [ブロッカー]

| # | 方式 | メリット | デメリット |
|---|---|---|---|
| **(1)** | **Native Installer**（公式推奨） | バックグラウンド自動更新が有効。`~/.local/bin/claude` に配置 | curl\|bash での再インストールが必要 |
| (2) | npm版のまま維持 | 何もしない | 今後 `npm update -g` を手動実行 |
| (3) | Homebrew Cask | brew管理に統一 | 自動更新なし |

**AI推奨**: (1) Native Installer。今後の手間が最小になる。

### ✍️ 回答記入欄
> 公式推奨のものに変更します

---

## Q2. Windows端末への接続方法 [方針確定]

Windows端末は手元になく、リモート操作不可。

- (a) 手順書だけ用意してくれれば自分で実行する（推奨）
- (b) リモートデスクトップ等で繋げばエージェントが直接操作可能（接続情報を共有する必要あり）
- (c) 後回しでよい（Mac側だけ進める）

**AI推奨**: (a)。

### ✍️ 回答記入欄
> A

---

## 次のアクション

Q1 が決まれば Mac 側のアップデート（または切替）を実施。Q2 が (a) なら、Windows 用の手順書を `claude_code_update_windows.md` として出力して終了。
