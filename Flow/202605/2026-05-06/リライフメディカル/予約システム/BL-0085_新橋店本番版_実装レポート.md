# BL-0085 新橋店本番版 実装レポート (5/6)

PoC `c5med-booking` を新橋店専用本番リポジトリ `c5med-shimbashi` として複製し、
田中さんから受領した新デザイン (`c5med-design.zip` / `C5med_LP.html`) を
React/Next.js に踏襲した。LINE 相談ボタンは指定の `https://lin.ee/y4yB6lD` に接続。

## 配置

- 新規リポジトリ: `/Users/rikutanaka/RestaurantAILab/c5med-shimbashi`
- ベース: PoC `c5med-booking` を rsync で複製 (`.git` / `node_modules` / `.next` / `.vercel` / `.env*.local` を除外)
- PoC の `.env` は誤参照防止のため `.env.poc-reference` にリネーム
- ポート: `3005` (PoC の 3004 と衝突しないように `package.json` を変更)
- パッケージ名: `c5med-booking` → `c5med-shimbashi`

## 実装方針 (田中さん 5/6 確定)

| 項目 | 確定内容 |
|---|---|
| 配置フォルダ | `RestaurantAILab/c5med-shimbashi` |
| 初期化方法 | PoC を丸ごと複製して新規リポジトリ化 |
| 環境変数 | `.env.local` は後日田中さんから受領 |
| 店舗モード | 新橋店専用にハードコード (`SHIMBASHI_STORE_ID`) |
| LINE ボタン | `https://lin.ee/y4yB6lD` |

## 主な変更ファイル

| パス | 内容 |
|---|---|
| `src/app/page.tsx` | 受領 LP (Hero / Reasons / Empathy / Needle-Free / Booking / Store / Nurse / Serum / Pricing / Flow / Monitor / FAQ / Final / Footer) を React 化。calendar mockup は PoC の `<BookingWidget storeId="shimbashi">` で置換 |
| `src/app/layout.tsx` | next/font に `Shippori_Mincho_B1` / `Italiana` を追加 |
| `src/app/globals.css` | 受領 LP の `:root` 変数 (`--ivory` / `--rose` / `--rose-deep` / `--ink` 等) を Tailwind v4 と共存する形で定義。広い画面では `.lp-frame` で中央寄せ + dark backdrop |
| `src/app/stores/[slug]/page.tsx` | 旧 multi-store URL を `/` に redirect (PoC ブックマーク互換) |
| `src/components/lp/LineButton.tsx` | LINE 相談ボタン。`primary / secondary / ondark` の 3 variant。すべて `LINE_CONSULT_URL` (`https://lin.ee/y4yB6lD`) に `target="_blank" rel="noopener noreferrer"` で遷移 |
| `src/components/lp/LpFAQ.tsx` | 受領 LP の FAQ アコーディオン (Q.NN 番号 / Shippori Mincho タイトル) を React state で実装 |
| `src/components/lp/StickyCTA.tsx` | 画面下部追従 2 ボタン (カレンダー予約 / LINE 相談) |
| `src/components/lp/SectionHead.tsx` | eyebrow → title → lead の共通見出し |
| `src/lib/constants.ts` | `SHIMBASHI_STORE_ID` / `SHIMBASHI_STORE_NAME` / `LINE_CONSULT_URL` |
| `src/lib/db/seed.ts` | 新橋店 1 店舗のみ。`SHIMBASHI_CALENDAR_ID` env 未設定時は seed 失敗 (PoC カレンダー誤投入防止)。コースは初回体験 (¥9,980 / ¥13,980) を先頭に追加 |
| `.env.example` | 新橋店本番に必要な変数 (`SHIMBASHI_CALENDAR_ID` / `SHIMBASHI_PHONE` 等) を整理 |
| `README.md` | 新リポジトリの由来・ローカル起動・seed 手順を記載 |
| `public/lp/{images,uploads}/` | 受領 LP のアセットを格納 (元 PoC の `public/images/` は admin 等で利用するため温存) |

## 流用 (変更なし)

- `src/app/api/{availability,book,courses}/route.ts` — 予約 API
- `src/app/admin/*` — 管理画面 (店舗 / コース / サインイン)
- `src/components/{Booking,Course,Weekly}*.tsx` — 予約 UI
- `src/lib/{slot-generator,email,google-calendar,validations,db/{schema,index}}.ts`
- `drizzle/` (マイグレーション)
- `tests/` (Playwright E2E)

予約フローは PoC と完全に同一。LP 内 `<BookingWidget storeId="shimbashi" />` でそのまま利用。

## デザイン踏襲ポイント

- **モバイル特化** (max-width 480px、広い画面では中央寄せ + 暗背景フレーム)
- **カラー**: ivory (`#fbf6ef`) / rose-deep (`#a85b4b`) / ink (`#2b2420`) ベース
- **フォント**: Shippori Mincho B1 (見出し) / Italiana (eyebrow) / Cormorant Garamond (数値) / Noto Sans JP (本文)
- **重ね合わせ画像レイアウト** (Hero / Empathy / Nurse / Monitor) を React で再現
- **CTA 構造**: カレンダー予約 (primary) / LINE 相談 (secondary) を全セクションで一貫

## 検証

- `eslint` (`src/**/*.{ts,tsx}`): **0 errors / 0 warnings**
- `tsc --noEmit`: **0 errors**
- `bun install`: success
- `next build` は `DATABASE_URL` 必須のため `.env.local` 受領後に実施 (page は `force-dynamic` で SSR、build 時 DB 接続は不要なはずだが Neon HTTP 依存があるため env が要る)

## 田中さんへの引き継ぎ事項

1. **`.env.local` 受領後に必要な作業**
   - `cp .env.example .env.local` で新橋店アカウントの値を埋める
   - `SHIMBASHI_CALENDAR_ID` (新橋店 Gmail で作成した Google Calendar ID) は必須
   - `bun run src/lib/db/seed.ts` で新橋店 store row + コースを投入
   - `/admin` にログインし、営業時間・休日・コース紐付けを最終確認

2. **デプロイ**
   - PoC とは別の Vercel project を新規作成
   - `NEXTAUTH_URL` を本番ドメインに切替

3. **LP の最終調整候補** (実装内に `※実際の保管・解凍・管理工程に合わせて文言は最終調整。` のような note あり。受領 HTML の表記をそのまま反映済みだが、薬機表現等で再確認推奨)

4. **店舗住所・電話番号**
   - 受領 LP に基づき `〒105-0004 東京都港区新橋4-9 新橋プラザビル1208 / 新橋駅徒歩4分` をハードコードした
   - PoC seed には `新橋プラザビル 1208 / JR「新橋駅」烏森口 徒歩3分 / 090-1341-5704` の表記もあったため、最終的にどちらを正とするか確認が必要

## 未確認事項

- 新橋店本番版が PoC と同じ Neon プロジェクトを共有するか別プロジェクトにするか
  → 現状は env で切り替えれば両方に対応可能。デフォルトは `.env.example` を別プロジェクト想定で記載
- `lin.ee` 短縮 URL は LINE Notify で誰でも友達追加できるリンクか確認
- 受領 LP のフッター "C5med BEAUTY" 表記と PoC の "C5med Beauty" の表記揺れ (どちらに揃えるか)
