---
date: 2026-06-26
role: master
mode: parallel_dev_with_retro
projects:
  - id: marke_dash
    name: マーケダッシュボード
    repo: ~/RestaurantAILab/Dashboard
    type: feature_add_to_existing
    branch_rule: feature/marke-dash-...（main 直接コミット禁止）
  - id: keiei_dash
    name: ユニゾンさん_経営ダッシュボード
    repo: TBD（新規・独立 Node.js）
    type: greenfield_node
    deliverable: 要件定義 + Mock
  - id: video_gen
    name: ユニゾンさん_動画生成プラットフォーム
    repo: TBD（新規・独立 Node.js）
    type: greenfield_node
    deliverable: 動くプロトタイプ（裏でデータ処理あり）
---

# 6/26 マスター調整 — 3 並行開発 + プロセス振り返り

## 今日のゴール

1. **3 つのアプリを並行で前進させる**（全て 0 から）
   - マーケダッシュボード: 既存ダッシュボードへの機能追加。**新 Feature ブランチ必須**。要件再確認 → プロトタイプ着手。
   - 経営ダッシュボード（ユニゾンさん案件）: **独立 Node.js アプリ**。要件定義 + Mock。
   - 動画生成プラットフォーム（ユニゾンさん案件）: **独立 Node.js アプリ**。Mock ではなく **裏でデータが動く実動プロトタイプ**。
2. **一日の最後に開発プロセスの振り返り**を行い、ミニタキオン UI への反映観点を整理する（実装は後日）。

---

## 作業タイプの 3 分類（今日の運用観察対象）

| タイプ | 主担当 | 性質 | UI で欲しいもの（仮説） |
|---|---|---|---|
| A. 要件定義 | 田中 + AI 協働 | 対話で詰める。AI が叩き台 → 田中が補正 | 要件ドキュメントの編集 + 差分レビュー UI、未確定項目のハイライト |
| B. 田中による確認 / 意思決定 / レビュー | 田中（決裁） | AI から見せられた成果物に O/× 判断 | 並列タスクの「要確認」キュー、Mock のプレビュー埋め込み、deliverable 単位の承認ボタン |
| C. AI による実装作業 | AI（自走） | コード生成・ファイル編集・テスト | バックグラウンド実行ジョブ表示、進捗 streaming、エラー時の介入導線 |
| D. 外部連携作業 | 田中（手動） | API キー取得、媒体ログイン情報、規約承諾 | 「お願い項目」リスト + 入手後の secret 注入導線、外部依存の可視化 |

> 本日の運用で各タイプがどう発生したか、`Flow/202606/2026-06-26/_orchestration/process_retro_2026-06-26.md` に逐次メモ → 夜にまとめる。

---

## 各案件の今日の進め方

### 1. マーケダッシュボード（既存 → 機能追加）

- **状態**: 要件定義書 v1（2026-06-19）+ PO 判断 5 項目（2026-06-25）確定済。次アクション = BL-0098（Prisma schema + 媒体マスタ seed）
- **今日のスコープ（提案）**:
  1. 要件定義書 v1 + PO 判断差分を再読し、矛盾・抜けがないかチェック（タイプ A/B）
  2. `RestaurantAILab/Dashboard` リポで `feature/marke-dash-BL-0098-prisma-schema` 系の Feature ブランチを切る（タイプ C）
  3. BL-0098 のスキーマ案を AI が叩き台で生成 → 田中レビュー（A → B → C）
- **田中の作業（D 系）**:
  - 媒体ID/PW の格納方針再確認（既に S-03 で受入可）
  - レストランボード / 媒体管理画面の取得経路再確認
- **保管先**: `Flow/202606/2026-06-26/RestaurantAILab/マーケダッシュボード/`

### 2. ユニゾンさん_経営ダッシュボード（新規・要件定義 + Mock）

- **状態**: 0 から。要件すら未定義。
- **今日のスコープ（提案）**:
  1. ヒアリングメモ / ミーティング記録の有無を確認（→ 既存 `Stock/0.Other/ユニゾンさん/` を参照。同プロジェクト配下の新サブ成果物として扱う。BL-0001 は動画系で別建て）
  2. 経営ダッシュボードの **要件定義 v0 ドラフト**作成（タイプ A）
     - 想定ユーザー / 主要 KPI / 画面構成 / データソース / 認証
  3. **Mock UI**（Next.js or Vite + React 想定）を `~/aipm_v0` 外の新規ディレクトリで初期化（タイプ C）
     - 認証なし、データは固定 JSON、グラフは recharts 等
- **田中の作業（D 系）**:
  - ユニゾンさんからの追加情報（KPI 定義、対象企業、データソース）有無確認
- **保管先（要件）**: `Flow/202606/2026-06-26/0.Other/ユニゾンさん/経営ダッシュボード/`
- **コード置き場**: 別途決定（既存 `~/RestaurantAILab/...` パターンに倣う候補）

### 3. ユニゾンさん_動画生成プラットフォーム（新規・実動プロトタイプ）

- **状態**: 既存 `Stock/0.Other/ユニゾンさん/backlog/BL-0001.yaml` に「複数の動画生成 API 比較 → モデル選定 → プロンプトチューニング」あり。今日はその **プロトタイプ実装**フェーズとして接続。
- **今日のスコープ（提案）**:
  1. 動画生成 API 候補のショートリスト確認（Runway / Pika / Luma / OpenAI Sora / etc.）（タイプ A/D）
  2. プロトタイプ要件 v0（入力フォーム → API 呼び出し → 動画 URL 返却）（タイプ A）
  3. Node.js プロジェクト初期化、API 1〜2 個でも繋いで動かす（タイプ C）
- **田中の作業（D 系）**:
  - 各 API キー取得（候補確定後）
  - 規約・商用利用可否確認
- **保管先（要件）**: `Flow/202606/2026-06-26/0.Other/ユニゾンさん/動画生成プラットフォーム/`
- **コード置き場**: 別途決定

---

## 並行運用方針（マスターとしての動き）

- **TaskCreate / TaskList**: セッション内の TODO 追跡（タスク#1〜#6）
- **mt CLI**: BL 単位で永続管理。**今日は新規 BL を 2 件作成**（ユニゾンさん配下: 経営ダッシュボード / 動画プロトタイプ。動画は既存 BL-0001 と連携 or 子 BL 化を判断）。マーケダッシュボードは既存 BL-0098 等を活用
- **作業実装**: Agent ツール（`general-purpose` / `Plan`）を **並列スポーン** して 3 案件を進める。マスターは差分レビュー・整合性チェック・田中への質問取り回し
- **質問の上げ方**: 「`mt bl add-question <BL>`」で UI に流す（会話だけで済ませない）

---

## 振り返り（夜）チェックリスト

- [ ] 3 案件それぞれ、A〜D の各作業タイプが何回発生したかカウント
- [ ] 並列実行で「待ち時間」「混線」が起きた瞬間のメモ（時刻 + 状況）
- [ ] 田中の意思決定が必要だった瞬間と所要時間
- [ ] AI が暴走 / 迷子になった瞬間
- [ ] 外部連携（API キー取得など）が boundary になった点
- [ ] ミニタキオン UI で「あったら助かった」機能の列挙（仮説 → 検証材料）

→ 結果は `process_retro_2026-06-26.md` に書き、後日ミニタキオン BL に反映候補をまとめる。

---

## 関連ファイル

- 要件: `Stock/RestaurantAILab/マーケダッシュボード/docs/要件定義書_v1.md`
- 既存 BL: `Stock/0.Other/ユニゾンさん/backlog/BL-0001.yaml`（動画生成）
- ルール: `.cursor/rules/aios/00_aios_core.mdc`, `.cursor/rules/aios/ops/13_mini_tachyon_protocol.mdc`

## 未確定 / 田中に確認したいこと

- [x] プロジェクト名: 「ユニゾンさん」（2026-06-26 確定。既存 `0.Other/ユニゾンさん/` 配下に経営ダッシュボード / 動画生成プラットフォームを並置）
- [ ] 経営ダッシュボード / 動画プラットフォームの新規 Node.js リポ置き場（`~/aipm_v0` 外、命名規約）
- [ ] 動画生成プラットフォームは既存 BL-0001 を更新 or 新規 BL（プロトタイプ実装スコープ）として子分けするか
- [ ] 各案件の今日の停止条件（例: 「ここまで行けば一旦 OK」）
