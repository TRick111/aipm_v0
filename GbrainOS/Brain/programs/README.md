# programs/ — 横断ワークストリーム

## ここに置くもの
複数 projects を束ねる人生・事業 program。'森' の単位

## ここに置かないもの
個別プロジェクトは projects/

## ファイル命名
`<slug>.md`（小文字、ASCII or 日本語、空白はハイフン）

## 必須 frontmatter
`slug` / `type` / `created_at` / `updated_at`（その他 type 固有フィールドは schema.md 参照）

## See Also
- `../RESOLVER.md` — 全体の決定木
- `../schema.md` — ページ構造とテンプレート
