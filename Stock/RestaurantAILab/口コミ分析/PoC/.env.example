# Google Business Profile API 設定
#
# OAuth 2.0 認証を使用するため、API Keyは不要です
# 代わりに credentials.json と token.json が必要です
#
# セットアップ手順:
# 1. Google Cloud Console でプロジェクトを作成
# 2. My Business Business Information API と My Business Account Management API を有効化
# 3. OAuth 2.0 クライアントIDを作成（デスクトップアプリケーション）
# 4. credentials.json をダウンロードしてこのディレクトリに配置
# 5. npm run auth を実行して認証を完了
