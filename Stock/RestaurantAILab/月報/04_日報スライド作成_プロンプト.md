# 04: 日報スライドの作成（フェーズ4）（AI手順書）

> 目的/更新履歴は `00_月報作成_統合プロンプト.md` に集約済み。
> 本ファイルは **AIが参照して実行できる手順** のみを記載します。

---

## 入力（00から渡される前提）
- `{運営会社コード}`
- `{対象月}`
- `{開始日}`
- `{終了日}`
- `{出力ディレクトリ}`
- `{スライド構成}`（フェーズ3で生成済みの `月報スライド構成案.md`）
- `{入力日報}`: `./1_input/{運営会社コード}/DailyReport.csv`

---

## 目的（このフェーズの成果物）
- DailyReport.csv（スタッフ日報）から **4〜6枚のスライド** を作成し、`{スライド構成}` に追加する
- 月間を通じた日報の生の声（引用）を通じて、数字だけでは見えない **店舗の現場感** を伝える
- 週報（3〜5枚）より多めに取り、月間の傾向やテーマの変遷を表現する

---

## 参照
- 入力: `{入力日報}` （CSV: 営業日, 入力者氏名, ①来客特徴, ②改善ポイント）
- 追記先: `{スライド構成}`

---

## 手順（AIが実行）

### 1) DailyReport.csv を読み込み、対象月のエントリを抽出
- `営業日` 列で `{開始日}` 〜 `{終了日}` に含まれる日付のレコードを抽出
- 各エントリから `入力者氏名`、`①来客特徴`、`②改善ポイント` を確認
- 入力者氏名の姓のみを使用し、「さん」を付ける（例: 大泉　瞳 → 大泉さん）

### 2) テーマの抽出とスライド構成
日報の内容を読み通し、以下の観点から **4〜6個のテーマ** を抽出する：

| 観点 | 例 |
|------|-----|
| 店舗の様子・客層 | 海外ゲストの来店、常連客の利用、グループ客 |
| 顧客の行動・嗜好 | ウイスキー人気、2次会需要、仕事終わりの利用 |
| スタッフの様子 | 繁忙時の対応、閑散時の活用、チームワーク |
| 運営課題・改善 | 新人育成、人手不足、設備・メニューの改善提案 |
| トラブル | クレーム、オペレーションミス、外的要因（天候・交通） |
| 月間の変化・傾向 | 月初と月末の客層変化、季節の影響、リピーターの動向 |

**テーマ選定のルール**:
- 1スライドにつき **タイトル（見出し）は最大2つ**
- 各タイトルの引用は **できるだけ複数の日・複数の週に跨ぐ** ものを選ぶ（月報なので月間の傾向を示す）
- 1日分しか関係する引用がない場合はそれでもOK（無理に増やさない）
- 対象月の日報全体から、報告に値するトピックを優先的に選ぶ
- **月間を通じた傾向やパターン**を意識してテーマを選ぶ（単発の出来事より、繰り返し現れるテーマを優先）

### 3) 引用の作成ルール

#### 引用フォーマット
```markdown
> **{月}月{日}日（{曜日}）{姓}さん:**
> 「{引用テキスト}」
```

**具体例**:
```markdown
> **1月5日（月）大泉さん:**
> 「常連の男性10名が来店し、わさびジンソニックを気に入っている様子だった。」
```

#### 引用の絞り込みルール（重要）
- **タイトルに関係ある部分だけを引用する**。日報の1日分をまるまる引用しない
- 1つのエントリに複数の話題が含まれている場合、タイトルに関連する文だけを抜き出す
- タイトルと関係ない内容が混ざっている場合は、関係あるものだけを引用する（1個でもOK）
- 注文内容の羅列など、タイトルの趣旨に無関係な詳細はカットしてよい
- **月報では、月の前半/中盤/後半からバランスよく引用を選ぶ**（偏りを避ける）

### 4) スライドの構成

#### 書くもの
- スライドタイトル（`## スライドN: 店舗の様子① — {テーマA}と{テーマB}`）
- 見出し（`### {タイトル}`）：1スライドにつき最大2つ
- 引用（上記フォーマット）

#### 書かないもの
- **分析・考察・ポイントなどのコメンタリーは一切書かない**
- 「メッセージ」欄も不要
- データ表・数値分析も不要（日報スライドは定性情報のみ）

### 5) スライド構成案への挿入

#### 挿入位置
- **施策一覧（第N部）の直前** に新しい部として挿入する
- 部タイトル: `## 第{N}部：店舗の様子 — 日報より（{枚数}枚）`
- 施策一覧は最終部に繰り下げる

#### スライド番号の採番
- 既存の第5部（今月の総括）の最終スライド番号の次から採番
- 施策一覧のスライド番号もそれに応じてリナンバーする

#### フッターの更新
- ファイル末尾のフッター（`*本スライド構成案は…*`）の全体枚数を更新

### 6) 保存
- 更新した `{スライド構成}` を上書き保存する

---

## 出力形式（例: 5枚構成の場合）

```markdown
## 第6部：店舗の様子 — 日報より（5枚）

---

## スライド28: 店舗の様子① — 海外ゲストの来店と常連・グループ客

### Google検索を経路とした海外ゲストの来店

> **1月5日（月）大泉さん:**
> 「海外からのゲストであるスペイン語圏の家族と思われる男女5名がGoogle検索を通じて来店した。」

> **1月18日（日）大泉さん:**
> 「スイスからの海外ゲスト4名がGoogle検索を通じて来店し、日本に2週間滞在していた。」

> **1月27日（火）橋本さん:**
> 「英語圏からの観光客カップルがInstagramで店を見つけて来店した。」

### 常連グループの予約利用と2次会需要

> **1月5日（月）大泉さん:**
> 「常連の男性10名が来店し、わさびジンソニックを気に入っている様子だった。」

> **1月15日（木）橋本さん:**
> 「常連のお客様の来店が売上に貢献した。」

> **1月28日（水）大泉さん:**
> 「30代～40代のグループは、仕事終わりの何軒目かに当店を利用したようであった。」

---

## スライド29: 店舗の様子② — ウイスキー人気と多様な来店動機

（以下同様に続く）
```

---

## 注意事項

1. **「達成・未達成」の使い方**
   - 「達成」「未達」「達成率」は **明確な目標値に対して** 使う言葉。目標値が設定されている場合のみ使用する
   - 前年同月や前月との比較に「達成率」は使わない。「前年比+◯%」「前月比+◯%」と表現する

2. **スライド上の表記は日本語を使う**
   - カタカナ英語・英語表記はなるべく避け、日本語で表現する
   - 特に以下のような表現は使わない:
     - NG: 「サイズ」→ OK: 「規模」「人数」
     - NG: 「ケース」→ OK: 「件」「事例」
     - NG: 「カウント」→ OK: 「件数」「数」
     - NG: 「セールス」→ OK: 「売上」
   - 固有名詞（商品名・店舗名・サービス名など）はそのまま英語表記でOK
   - 一般に広く使われ日本語に置き換えると不自然なもの（KPI、SNS等）は許容する

3. **月報ならではの注意点**
   - 月間を通じた傾向やパターンを重視する（単発の出来事より繰り返し現れるテーマを優先）
   - 月の前半/中盤/後半からバランスよく引用を選ぶ
   - 週報の日報スライド（3〜5枚）より多め（4〜6枚）にして、月間の多様なトピックをカバーする

---

## チェックリスト（フェーズ4完了条件）
- `{入力日報}` (DailyReport.csv) が存在し、対象月のエントリがあること
- 4〜6枚のスライドが作成されていること
- 各スライドのタイトルは最大2つであること
- 引用はタイトルに関連する部分のみで、コメンタリー（ポイント・分析・メッセージ）がないこと
- 引用フォーマットが `**{日付}（{曜日}）{姓}さん:**` であること（「の日報」を含まない）
- 各タイトル配下の引用が、可能な限り複数の日・複数の週に跨いでいること
- 月の前半/中盤/後半からバランスよく引用が選ばれていること
- 施策一覧の前に挿入され、スライド番号が正しくリナンバーされていること
- `{スライド構成}` に保存されていること
