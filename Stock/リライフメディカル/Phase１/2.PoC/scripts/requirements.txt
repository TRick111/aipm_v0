# Core
tqdm>=4.66.0
pandas>=2.0.0
python-dotenv>=1.0.1

# Audio handling (optional at runtime for concatenation)
pydub>=0.25.1

# Google Generative AI SDK used as `from google import genai`
google-genai>=0.3.0
