# 定期実行（Cron / Cockpit autorun / launchd 常駐サービス）

## 背景
- 1日のレポート作成など、毎日/毎週/毎月の定型処理を手作業で行うと漏れや遅延が発生しやすい
- AI（主にCursor CLI）を活用して、定期タスクを自動実行・監視できる運用基盤を整える必要がある
- ユーザーの環境では Cron 以外にも Cockpit autorun と macOS launchd を併用しており、用途別の使い分けを一元管理する

## 目的
- 定期実行・常駐サービスの作成・管理を、AIOSプロジェクトとして一元管理する
- タスク定義、実行ログ、失敗時リカバリ手順を標準化し、運用コストを下げる

## 実行基盤の選択肢

| 基盤 | 向いている用途 | 主な特徴 | 代表ジョブ |
|---|---|---|---|
| Cron (crontab) | 1日数回のレポート生成等、単発で終わるバッチ | シンプル、PCスリープ中は走らない | 日次Gmail返信要否レポート |
| Cockpit autorun (`./cockpit autorun`) | AIエージェントを定期的にスポーン、対話を伴う処理 | Cockpit UI から状態確認・応答が可能。`waiting_confirmation` が溜まりやすい | （現在はなし。旧Markserv実行は launchd へ移行） |
| macOS launchd (`~/Library/LaunchAgents/*.plist`) | 24時間動き続けるべき常駐サービス（HTTPサーバ等） | `KeepAlive` で自動再起動。`StartCalendarInterval` で時刻指定実行も可 | Markserv 3ポート常駐（aipm-flow / markdowns-1 / dashboard-docs） |

### どれを選ぶか
- **常駐で動かしっぱなしにしたい / 落ちたら勝手に復活してほしい** → launchd
- **エージェント的な作業を定期発火させたい（成果物が会話履歴として残る、応答可能）** → Cockpit autorun
- **完全に自己完結したバッチを毎朝/毎週などに走らせたい** → Cron

> Cockpit autorun で常駐サービス（Markserv等）を回そうとすると、毎発火ごとに既存プロセスを kill→再起動する形になり、ページ遷移が不安定になる事例があった（2026-06-15）。常駐は launchd に倒す方針。

## ゴール（完了条件）
- Cronで動く定期実行ジョブの雛形（実行スクリプト + スケジュール + ログ）が運用可能
- 1日レポート作成などの実ジョブを再利用可能な形で追加できる
- 失敗検知と通知（例: LINE/メール/ログ監視）の基本導線が整っている

## 定期実行のルール
- まず手動実行で成功させてから、Cronなどの定期実行に登録する
- 1ジョブ1責務で小さく作る
- 実行ログを必ず残す
- 入力・出力・失敗時の対応をジョブ定義に明記する
- APIキーやトークンなどの秘密情報はコミットしない
- PCがスリープ/電源OFF中の場合、Cronは実行されない可能性がある前提で運用する

## 【最重要】2段階運用フロー（ジョブ案作成 → Cron登録）

> このプロジェクトでは、新規ジョブの追加を**必ず2段階に分ける**。
> 担当AIエージェントは、ユーザー依頼を受けた際にこのフローに従うこと。

### Step 1：ジョブ案を作成する（Cron登録はしない）

ユーザーから「○○するジョブを作って」「定期実行のジョブを追加して」と依頼された場合、AIは以下のみを行う：

1. `ジョブ一覧/` にジョブ説明Markdownを作成（テンプレートをコピーして埋める）
2. `scripts/` に実行スクリプトを作成
3. `ProjectIndex.yaml` と `log.md` を更新
4. **手動で1回実行**し、出力とログを確認
5. 手動実行の結果（成功 / 出力ファイル / ログ）をユーザーに報告
6. **この時点ではCronに登録しない**

### Step 2：Cronに登録する（明示依頼時のみ）

ユーザーから「このジョブをCronに登録して」「毎朝○時に動くようにして」と**明示的に**依頼された場合のみ、AIは以下を行う：

1. `crontab` を編集してジョブを登録
2. `crontab -l` の結果をユーザーに見せて確認させる
3. 該当ジョブMarkdownに「Cron登録済み」と実行時刻を追記
4. `log.md` にCron登録を記録

### 例外と確認

- 例外：ユーザーが明示的に「Step 1とStep 2を一気にやってください」と指示した場合のみ、両方をまとめて実施してOK
- 依頼が曖昧な場合、AIは「Step 1（案作成）のみで進めますか？それとも Step 2（Cron登録）まで実施しますか？」と確認すること

## 定期実行ジョブを作る際のルール
- `scripts/` に実行スクリプトや実行ラッパーを置く
- `ジョブ一覧/` にジョブ説明書をMarkdownで置く
- `logs/` にCronや手動実行のログを保存する
- `テンプレート/` にジョブ定義テンプレートを置き、新規ジョブ作成時はコピーして使う
- 新しいジョブを追加したら `ProjectIndex.yaml` と `log.md` を更新する
- **新規ジョブの追加は上記の「2段階運用フロー」に従うこと**

## フォルダの役割
| フォルダ | 役割 |
|---|---|
| `scripts/` | 実際に動くスクリプトや実行ラッパー |
| `ジョブ一覧/` | ジョブごとの目的・入力・出力・実行タイミングを説明するMarkdown |
| `logs/` | Cronや手動実行のログ保存先 |
| `テンプレート/` | 新しいジョブ説明書を作るための雛形 |

## launchd 常駐サービスのルール

- plist は `~/Library/LaunchAgents/jp.<サービス名>.plist` に置く（Labelもファイル名と同一）
- 必ず `KeepAlive` の `Crashed=true` で異常終了時のみ自動復活させる（`SuccessfulExit=false`）
- `ThrottleInterval` は最低 10 秒（再起動ループ防止）
- ログは `~/Library/Logs/<サービス名>/*.log` に保存
- 起動・停止コマンド:
  - 起動: `launchctl load ~/Library/LaunchAgents/<label>.plist`
  - 停止: `launchctl unload ~/Library/LaunchAgents/<label>.plist`
  - 再起動: `launchctl kickstart -k gui/$UID/<label>`
  - 状態: `launchctl list | grep <label>`
- 動的に対象を切り替えたい場合（月次でフォルダを切り替える等）は、別 plist で更新スクリプトを `StartCalendarInterval` 起動し、内部で `launchctl kickstart -k` する

## 関係者

| 名前 | 役職 | 役割 |
|---|---|---|
| 田中利空（ユーザー） | - | オーナー / 要件定義 / 運用判断 |
| AI（Cursor CLI） | 実行支援 | スクリプト作成・改善 / 運用補助 |

## 現在の状況・ネクストアクション
- 現状: プロジェクト初期化完了
- ネクストアクション:
  - [x] Cron実行対象タスク（例: 日次Gmail返信要否レポート）を1件作成
  - [x] 実行スクリプトの雛形を作成して手動実行テスト
  - [ ] `crontab` へ登録し、ログ出力/通知動作を確認

## 今後のプラン
- **Step 1**: 実行基盤の整備（実行ユーザー、環境変数、ログ先、通知先）
- **Step 2**: 日次レポート作成ジョブを最初の本番タスクとして実装
- **Step 3**: 週次/月次タスクへ展開し、共通テンプレート化

## 重要リンク
- crontab.guru: https://crontab.guru/
- Cron (man page): https://man7.org/linux/man-pages/man5/crontab.5.html
